# =============================================================================
# Away with Audrey — Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
# Never commit your .env file — it's already in .gitignore.

# =============================================================================
# Google APIs
# =============================================================================
# Get both keys from Google Cloud Console → APIs & Services → Credentials
# https://console.cloud.google.com/apis/credentials
#
# Enable these APIs in your project:
#   - Places API (New)
#   - Maps JavaScript API

# Server-side key for Google Places API (New) — used in API routes to fetch place details
GOOGLE_PLACES_API_KEY="your-google-api-key-here"

# Client-side key for Google Maps JavaScript API — loaded in the browser for map display
# Can be the same key as above, but consider restricting each by HTTP referrers vs IP
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key-here"

# =============================================================================
# App URL
# =============================================================================
# Base URL of the app — used for generating review/share links
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# =============================================================================
# Database (PostgreSQL via Neon)
# =============================================================================
# Get this from the Neon dashboard → your project → Connection Details
# https://console.neon.tech/
# Format: postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
DATABASE_URL="postgresql://user:password@ep-example-123456-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require"

# =============================================================================
# Auth.js (NextAuth v5)
# =============================================================================
# Generate a random secret: npx auth secret (or: openssl rand -base64 33)
AUTH_SECRET="generate-a-random-secret-here"

# Google OAuth credentials — from Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID
# https://console.cloud.google.com/apis/credentials
# Set the authorized redirect URI to: http://localhost:3000/api/auth/callback/google
AUTH_GOOGLE_ID="your-google-oauth-client-id.apps.googleusercontent.com"
AUTH_GOOGLE_SECRET="your-google-oauth-client-secret"

# =============================================================================
# Production only (Vercel)
# =============================================================================
# Set AUTH_URL to your production domain when deploying to Vercel
# AUTH_URL="https://your-app.vercel.app"
